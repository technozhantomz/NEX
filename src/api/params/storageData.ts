import { defaultQuote } from "./networkparams";

export const defaultSettings = {
    language: 'en',
    darkTheme: false,
    advancedMode: false,
    notifications: true,
    walletLock: 0,
    nodeAutoselect: true,
    defaultAsset: defaultQuote
};