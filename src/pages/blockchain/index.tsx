import type { NextPage } from "next";

import { Layout } from "../../common/components/PageLayout";

const Blockchain: NextPage = () => {
  return (
    <Layout title="Blockchain">
      <div>Blockchain Page</div>
    </Layout>
  );
};

export default Blockchain;
