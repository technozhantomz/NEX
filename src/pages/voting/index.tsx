import type { NextPage } from "next";

import Layout from "../../components/layout";

const Voteing: NextPage = () => {
  return (
    <Layout title="Voteing">
      <div>Voteing Page</div>
    </Layout>
  );
};

export default Voteing;
